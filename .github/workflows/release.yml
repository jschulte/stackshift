name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    name: Create Cox StackShift Release
    runs-on: CAI-Enterprise

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Extract version from tag
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Create release notes
        id: release_notes
        run: |
          cat > release-notes.md <<EOF
          ## Cox StackShift v${{ steps.get_version.outputs.VERSION }}

          ### 🎯 What is Cox StackShift?

          Internal Cox Automotive version of StackShift - a reverse engineering toolkit
          for transforming applications into fully-specified, enterprise-grade codebases.

          ### ✨ Features

          - **6-Gear Workflow**: Systematic reverse engineering process
          - **Plugin-based**: Interactive skills for Claude Code
          - **Osiris Support**: Documentation for ws-scripts infrastructure
          - **Upstream Sync**: Automated syncing with upstream StackShift
          - **Cox Customizations**: Tailored for Cox Automotive needs

          ### 📦 Installation

          \`\`\`bash
          # Install the plugin in Claude Code
          /plugin install DDC-WebPlatform/stackshift
          \`\`\`

          ### 📚 Documentation

          - [README](https://ghe.coxautoinc.com/DDC-WebPlatform/stackshift)
          - [Osiris Documentation](https://ghe.coxautoinc.com/DDC-WebPlatform/stackshift/tree/main/docs/osiris)
          - [Automation Guide](https://ghe.coxautoinc.com/DDC-WebPlatform/stackshift/tree/main/scripts/cox-automation)

          ### 🔄 Upstream Version

          Based on [jschulte/stackshift](https://github.com/jschulte/stackshift)

          ### 💬 Questions?

          Reach out to your engineering leadership or the platform team.
          EOF

          echo "notes_file=release-notes.md" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          name: Cox StackShift v${{ steps.get_version.outputs.VERSION }}
          body_path: release-notes.md
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Summary
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ Release Created Successfully"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "Version: v${{ steps.get_version.outputs.VERSION }}"
          echo ""
          echo "View release:"
          echo "https://ghe.coxautoinc.com/DDC-WebPlatform/stackshift/releases/tag/v${{ steps.get_version.outputs.VERSION }}"
          echo ""
